import { AlertItem } from '../types';

// Sin datos simulados: la aplicación arranca vacía y se puebla en tiempo real
// desde el feed RSS configurado en RSS_BUNDLE (ver /api/feed-proxy).
export const INITIAL_REALTIME_ALERTS: AlertItem[] = [];
